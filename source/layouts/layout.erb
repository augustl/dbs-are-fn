<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <% if yield_content(:title).blank? %>
      <title>DBs are fn!</title>
    <% else %>
      <title><%= Sanitize.clean(yield_content(:title)) %> (DBs are fn!)</title>
    <% end %>
    <%= stylesheet_link_tag "screen" %>
    <link href="/atom.xml" rel="alternate" title="August Lilleaas' blog" type="application/atom+xml">
  </head>
  <body>
    <div class="header-block">
      <div class="content-block">
        <ul class="inline-list">
          <li><a href="/" class="large-header-title-text">DBs are fn</a></li>
          <!-- <li><code>f(world_state<sub>1</sub>)</code> -> <code>world_state<sub>2</sub></code></li> -->
          <li>A blog about functional databases, by <a href="http://augustl.com/about">August Lilleaas</a></li>
        </ul>
      </div>
    </div>
    <div class="content-block">
      <%= yield %>
    </div>
  </body>
</html>
